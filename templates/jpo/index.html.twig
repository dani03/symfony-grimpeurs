{% extends 'base.html.twig' %}

{% block title %}Hello JpoController!{% endblock %}

{% block button %}
      <button class="bg-blue-500 m-4 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded"><a  href="{{ url("jpo_create") }}" >creer une journée porte ouverte</a></button>
{% endblock %}
{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
          <a href="#" class="w-[30rem] border-2 border-b-4 border-gray-200 rounded-xl hover:bg-gray-50">
   {% for jpo in jpos %}
                 <div class="grid grid-cols-6 p-5 gap-y-2">

                        <!-- Profile Picture -->
                        <div>
                               <img src="https://picsum.photos/seed/2/200/200" class="max-w-16 max-h-16 rounded-full" />
                        </div>

                        <!-- Description -->
                        <div class="col-span-5 md:col-span-4 ml-4">

                               <p class="text-sky-500 font-bold text-xs">{{jpo.title}}</p>

                               <p class="text-gray-600 font-bold"> {{ jpo.lieu}}</p>


                        </div>



                 </div>

    {% endfor %}
          </a>

</div>
{% endblock %}
